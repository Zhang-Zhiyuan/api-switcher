# API切换器

一个强大的 API 管理和切换工具，支持多种 AI API 提供商，并具有智能错误自动恢复功能。

## ✨ 主要功能

### 1. API 管理
- 支持多个第三方 API 提供商（DeepSeek V4、Kimi、GLM、自定义 OpenAI-compatible / Anthropic-compatible）
- 快速切换不同的 API 配置
- 支持 Claude Code / Codex CLI 官方账号快照切换
- 支持切换前预览和配置健康检查
- 安全的密钥管理
- 配置导入/导出
- 在 Profile 编辑器中测试连接、刷新模型列表、查看 provider 专属提示

### 2. 自动续跑功能
- 自动检测任务是否完成
- 智能续跑未完成的任务
- 支持 Claude Code 和 Codex CLI
- 可配置的续跑策略

### 3. Git 版本管理 ⭐
- **自动创建 Git 快照**（默认开启）
  - 对话开始时自动创建快照
  - 错误恢复前自动创建快照
  - 自动续跑时自动创建快照

- **本地版本管理**
  - 自动检测并初始化本地 Git 仓库
  - 已有仓库会直接复用，不覆盖 remote、分支或用户身份配置
  - 使用本地提交记录保存代码状态，不依赖远端仓库
  - 已有全局实名 Git 身份时继续沿用；缺少身份时才写入本地兜底身份
  - 支持查看提交历史
  - 回滚前自动保留安全标签，方便找回回滚前版本

### 4. 错误自动恢复 ⭐
- **智能识别 10 种 API 错误类型**
  - 内容超长
  - 速率限制
  - 认证错误
  - 权限错误
  - 配额超限
  - 模型过载
  - 超时错误
  - 网络错误
  - 流式连接中断（包括 Codex compact 任务断流）
  - 服务器错误
  - 无效请求

- **5 种自动恢复策略**
  - 压缩并继续（内容超长）
  - 等待后重试（速率限制）
  - 指数退避重试（服务器、网络或流式连接问题）
  - 友好提示（认证/权限/配额）
  - 中止执行（无效请求）

- **详细的错误统计**
  - 可视化统计卡片
  - 按错误类型分析
  - 错误时间线
  - 导出详细报告

### 5. SSH 与浏览器 Profile
- 把本机第三方 API 或官方账号快照推送到 SSH 服务器
- 从 SSH 服务器拉取第三方 Claude/Codex API 配置
- 在 SSH 服务器上检查、安装/修复、暂停和卸载 Claude/Codex 远端自动续跑
- 支持把 `HF_TOKEN`、Google Drive/Gemini、代理等环境变量永久写入或删除于本机 Windows 用户、所选 SSH 登录用户
- 管理 Chrome / Edge 托管 Profile，用于本机隔离 ChatGPT / Claude 登录态
- 支持托管浏览器 Profile 的加密迁移、站点数据清理和整目录重置

### 6. Win11 AI 代理
- 支持从订阅链接拉取 Clash/mihomo 节点；服务端拒绝常见客户端标识时会自动切换兼容标识重试
- 链接临时失效时保留最后一次成功缓存，不会因为拉取失败清空当前可用节点
- 支持导入本地 Clash/mihomo YAML，源文件移动或删除后仍可从托管缓存恢复
- mihomo 内核会在代理可用后仅后台刷新官方版本元数据，不与 Codex 首次连接争抢带宽；“更新内核”按钮才下载发行包，并校验大小、SHA-256 digest、平台架构和实际版本，运行中只暂存且不会自动断网重启
- 运行时会把当前节点和最多 4 个合格备用节点交给 mihomo `fallback` 组，以 OpenAI API 真实无认证响应做端到端健康检查；首次失败即触发复检，新连接自动转到首个可用备用节点
- Win11 启动验证并行检测 OpenAI API、ChatGPT、Claude 和 Gemini，并仅给内核故障切换一次有上限的初始化复检；逐节点长会话深测留在手动体检与后台订阅更新，不再卡住日常启动
- 默认兼容分流也会为代理节点使用大陆可达的加密 DNS 引导，AI/自定义代理域名的 DNS 则显式通过 `AI-PROXY`；同时开启多 IP 并发拨号和 TCP keepalive，减少 IPv4/IPv6 路径不均与长会话 NAT 超时
- 启动时只补齐本工具明确拥有且缺失的代理环境变量，不覆盖用户另行设置的非空值；若确认是本工具留下的死代理，会自动恢复启动前的 Windows、VS Code 和系统代理设置
- Windows 本机 mihomo 运行日志改为有界诊断：启动前按大小上限轮转历史文件，正常运行不再持续记录目标域名，健康状态由本机控制器提供
- 全新 SSH 服务器会先只安装受管 mihomo 内核，再执行隔离节点验证；内核下载失败时保留已有可用版本，不会破坏正在工作的代理配置
- 自动选优、自动刷新和热更新始终排除香港节点；香港节点仅允许手动选择
- “测速勾选/筛选”先要求本机连续 TCP 可达，再通过隔离临时代理三轮验证 OpenAI API、ChatGPT 出口、Claude 和 Gemini；短探针通过后，对最多 5 个候选执行两轮 1 MiB 下载、512 KiB 上传完整性检查及无 token 的官方 compact 路径预检，验证期间不会切换系统代理或当前运行节点
- 后台订阅热更新和自动备用节点同样必须先通过上述隔离深测，失败会保留当前运行节点；深测约使用 3.2 MiB/候选，是 Codex 长会话网络风险筛查，不代表真实账号 compact 一定成功
- 新增可选“严格隐私（应用层）”：凡是已经进入 mihomo 的公网流量都会 fail-closed 走节点，代理内部域名解析使用 DoH 并关闭 IPv6；严格模式下订阅拉取不会绕过失效代理直连
- 该功能仍不是 VPN/TUN，无法强制接管忽略系统代理的程序、系统 DNS 或任意 UDP；切换后需重启 Codex、Claude Code、VS Code 和终端。托管 Chrome/Edge 在严格模式生效时会显式绑定本机代理、禁用 QUIC，并限制 WebRTC 非代理 UDP
- 本地 Clash/mihomo YAML 只导入 `proxies` 节点，不继承原文件的 `dns`、`tun`、`fake-ip` 等顶层隐私设置

## 🚀 快速开始

### 下载和安装

1. 下载 `API切换器.exe`
2. 双击运行即可，无需安装其他依赖

> 默认使用 `python build_exe.py` 打包为单文件 `dist/API切换器.exe`；只有调试依赖收集问题时才需要 `python build_exe.py --onedir` 生成文件夹版。
>
> 正式发布使用 CPython 3.12，并先执行 `python -m pip install -r requirements-release.txt`；该清单锁定了已验证的完整构建依赖。随后运行 `python release_check.py --build`，会依次执行静态检查、全量测试、单文件构建和隔离启动冒烟验证。

### 首次使用

1. **添加 API 配置**
   - 点击"添加配置"按钮
   - 选择 API 提供商
   - 输入 API 密钥和其他配置
   - 保存

2. **切换 API**
   - 在配置列表中选择要使用的配置
   - 点击"应用"按钮
   - 系统会自动更新相应的配置文件

3. **启用错误自动恢复**（推荐）
   - 切换到"通用设置" Tab
   - 找到 Claude Code 或 Codex CLI
   - 点击"设置"按钮
   - 勾选"启用错误自动恢复"
   - 勾选"启用自动 Git 快照"（默认已开启）
   - 设置最大恢复次数为 3
   - 点击"保存"

## 📁 数据存储位置

软件自身数据统一保存在当前 Windows 用户的 Roaming 配置目录：

```text
%APPDATA%\API切换器\
```

这里包含 Profile 列表、备份、日志、使用统计、托管浏览器 Profile，以及 keyring 不可用时的 DPAPI 加密密钥备份。可以通过环境变量 `API_SWITCHER_DATA_DIR` 指定自定义数据目录。首次运行新版时，会把旧的项目内 `storage` 目录内容复制迁移到新位置，已存在的新数据不会被覆盖。

数据目录选择优先级：

1. 环境变量 `API_SWITCHER_DATA_DIR`
2. 程序同目录的 `data_dir.txt`，或用户配置目录里的 `data_dir.txt`（可在“通用设置 > 数据存储”里选择目录生成）
3. 程序同目录的 `portable.flag` 或环境变量 `API_SWITCHER_PORTABLE=1`，使用程序旁边的 `data` 目录
4. Windows Roaming 目录 `%APPDATA%\API切换器`
5. LocalAppData / 临时目录 fallback

“通用设置 > 数据存储”可以打开当前数据目录、复制路径、选择自定义目录、启用便携模式或恢复默认；更改目录会复制当前数据，并在下次启动后生效。

## 🌱 永久环境变量

顶部“HF_TOKEN 等”按钮或“环境变量”页可以把 `HF_TOKEN`、`OPENAI_API_KEY`、Google Drive/Gemini、代理等变量写入当前 Windows 用户环境变量，也可以按变量名删除。支持从已保存的 Claude/Codex API Profile 或本机已有环境变量导入密钥/Token。写入或删除后，新打开的 PowerShell/CMD/终端会自动读取最新状态，已经打开的终端需要重开。

“环境变量 > SSH 登录用户”会把变量写入所选服务器的当前 SSH 登录用户 HOME：密钥保存在 `~/.api_switcher_env`，并在常见 shell 启动文件中自动 source；也可以复用同一套导入来源，把本机保存的 API Key 或已有 Google Drive token 推送到远端。删除时只移除对应变量行，其他变量会保留，不会修改系统级 `/etc/environment`。

## 🔐 跨电脑迁移 Profile

在“备份管理”中使用“导出 ZIP”可以生成完整本地配置 ZIP。它面向本机配置备份/迁移，包含已保存的 Claude/Codex API Profile、官方账号快照、SSH 服务器、浏览器 Profile 元数据、活动选择，以及这些条目引用的 API Key、账号 token、SSH 密码和私钥口令。ZIP 内的密钥数据会用迁移密码加密；导入 ZIP 时会先显示包内 Profile/密钥数量，同名 Profile 会被替换，不同名 Profile 会保留。导入过程具有事务回滚保护，失败时会恢复 Profile、相关密钥和环境检测设置；同时创建的普通备份仅保护 Claude/Codex/VS Code 客户端运行配置，不包含可独立恢复的密钥快照。

在“备份管理”中使用“导出迁移包”可以生成加密的 `.asxprofile` 文件。迁移包包含 Claude/Codex/SSH 的第三方 API Profile 元数据、API Key、SSH 密码、SSH 密钥口令，以及浏览器 Profile 目录中的 Cookies、Local State、本地存储、IndexedDB、Session Storage、Service Worker 等登录态相关数据。导入到另一台电脑时，输入同一个迁移密码，软件会把密钥重新写入新电脑的本机凭据存储，并把浏览器数据恢复到本机托管的 `browser_profiles` 目录。

浏览器迁移会跳过 Cache、崩溃日志、SingletonLock 等临时/锁定文件。Chromium 在 Windows 上可能使用系统账号加密 Cookies，完整目录迁移也可能受浏览器自身加密机制限制；如果导入后登录态不可用，需要在新电脑重新登录一次。

浏览器 Profile 使用独立 `user-data-dir` 和 `Default` 分区启动，可隔离 Cookies、本地存储、IndexedDB、缓存、扩展和历史等站点数据。启动时可以固定窗口尺寸和语言代码，减少同一台机器上的环境差异；但网页通常不会读取一个稳定的“设备 ID”，而是综合 IP、系统、浏览器版本、显卡/WebGL、字体、Canvas、Audio、时区、屏幕等信息形成指纹。因此跨电脑迁移可以尽量保留登录态和站点状态，但不能保证网页看到完全相同的设备指纹。

## 📖 使用场景

#### 场景 1: 长时间对话
当使用 Claude Code 进行长时间对话时，如果遇到内容超长错误，系统会自动执行压缩命令并继续，无需手动干预。

#### 场景 2: 频繁调用
当 API 请求过于频繁触发速率限制时，系统会自动等待指定时间后重试，确保任务顺利完成。

#### 场景 3: 服务器不稳定
当遇到服务器繁忙、超时、网络断开或 `remote compact task` 流式连接中断/上游 503 重置时，系统会使用指数退避算法自动重试；如果 compact 任务断流，会重新触发压缩再继续。

#### 场景 4: 代码版本管理
系统会在对话开始和错误恢复前自动创建本地 Git 快照，如果代码出现问题，可以轻松回滚到之前的任意版本。这个功能面向本地 vibe coding 保护，不要求配置 GitHub 或远端仓库；如果项目本身已经配置了远端和实名身份，也会原样保留，不影响你继续手动 push。执行回滚前还会创建安全标签，便于找回回滚前的状态。

## 🎯 支持的 API 提供商

- **DeepSeek V4** (Codex OpenAI-compatible / Claude Code Anthropic-compatible)
- **Kimi** (Moonshot OpenAI-compatible)
- **智谱 AI / Z.ai** (GLM Coding Plan)
- **其他兼容 OpenAI API 的提供商**

## 🔧 系统要求

- **操作系统**: Windows 10 / Windows 11（Win11 系统代理功能建议使用 Windows 11）
- **架构**: 64 位
- **磁盘空间**: 约 100 MB
- **网络**: 需要互联网连接

## 💡 使用技巧

### 1. 配置管理
- 使用有意义的配置名称，方便识别
- 定期备份配置（导出功能）
- 为不同项目创建不同的配置

### 2. 错误恢复
- 建议启用错误自动恢复功能
- 设置合理的最大恢复次数（推荐 3-5 次）
- 定期查看错误统计，了解 API 使用情况

### 3. 自动续跑
- 根据任务类型选择保守模式或激进模式
- 自定义未完成模式和阻塞模式
- 配合错误恢复使用，效果更佳

### 4. 启动速度
- 主窗口采用按需加载页面：启动时先加载常用页，SSH、会话迁移、日志等页面在首次打开时再加载。
- 普通启动会先显示轻量启动窗，给双击操作即时反馈；排查启动问题时可传入 `--no-splash` 跳过。
- 默认构建产物是单文件 `dist/API切换器.exe`，运行时不需要旁边放 `_internal` 或其他依赖目录。

## 📊 错误统计功能

点击"错误统计"按钮可以查看：
- 总错误数、成功恢复数、恢复成功率
- 按错误类型的详细统计
- 最近的错误详情
- 导出详细报告

## ⚠️ 注意事项

### 安全性
- API 密钥存储在本地，请妥善保管
- 不要将配置文件分享给他人
- 定期更换 API 密钥

### 隐私
- 本应用不会上传任何数据到服务器
- 所有配置和日志都存储在本地
- 不会收集用户信息

### 兼容性
- 确保 Claude Code 或 Codex CLI 已正确安装
- 配置文件路径需要正确
- 某些杀毒软件可能误报，请添加信任

## 🐛 故障排除

### 问题 1: 应用无法启动
**解决方法**:
- 检查是否被杀毒软件拦截
- 尝试以管理员身份运行
- 查看日志文件（在应用数据目录）

### 问题 2: 配置无法保存
**解决方法**:
- 检查文件权限
- 确保配置目录存在
- 检查磁盘空间

### 问题 3: 错误恢复未触发
**解决方法**:
- 确认已启用错误恢复功能
- 检查 Hook 脚本是否正确安装
- 运行验证脚本（如果有源码）

### 问题 4: API 切换不生效
**解决方法**:
- 检查配置文件路径是否正确
- 确认 API 密钥有效
- 重启相关应用（Claude Code/Codex CLI）

## 📞 获取帮助

### 文档
- 查看应用内的帮助文档
- 源码环境可运行 `python release_check.py` 做发布前检查

### 日志
- 应用日志位置: `%APPDATA%\API切换器\logs\`
- 错误恢复日志: `~/.claude/error_recovery_log.jsonl` 或 `~/.codex/error_recovery_log.jsonl`

### 常见问题
查看文档中的故障排除部分

## 🔄 更新日志

### v2.4.10 (2026-08-27) - 当前版本
- ✅ 订阅/API/内核下载会在请求前识别仅存在于 WinINET 的失效回环代理并立即旁路；能确认由本工具写入时恢复完整 Windows、VS Code、系统代理检查点，来源不明时只提示并提供确认清理
- ✅ 未知失效系统代理的一键清理只关闭 `ProxyEnable`，保留 `ProxyServer`、PAC、自动检测和其他代理原值；端口恢复监听或配置发生变化时不会清理
- ✅ 订阅直连失败后继续通过最多 5 个已有受管节点的一次性隔离代理轮换下载；隔离严格 DNS 现绑定真实临时组名，完成后不改当前节点、系统代理或登录状态
- ✅ Win11 新启动/登录自启增加有界 Codex 验证：两条 Codex 入口都失败才停止并恢复启动前设置，任一入口可用则保留代理并明确提示部分可达
- ✅ 后台内核任务只查版本元数据；手动下载会复用已校验暂存文件并避免并发旧响应覆盖新状态，当前与待更新内核均通过真实配置语法检查
- ✅ API 自动解析增强带 `&` 的引号值和查询参数兼容，裸 Codex URL 自动补 `/v1` 时不再丢参数；Claude/Codex JSON、Shell、CMD、PowerShell 与 OpenCode 格式继续通过回归
- ✅ 状态反馈会明确区分受管 `17897` 与其他 WinINET 本机端口及其监听状态，本工具不会因端口不同而自动修改外部代理

### v2.4.9 (2026-08-27)
- ✅ Win11 已有可用 mihomo 时立即启动，官方版本检查改为代理就绪后后台执行；最新内核只校验暂存，不主动重启或中断 Codex
- ✅ AI 连通性改为 4 目标并行探测，启动仅等待一次有上限的 `fallback` 初始化复检，不再因自动串行深测最多 10 个节点而卡住数分钟
- ✅ 故障切换改为直接检查 OpenAI API，5 秒超时、10 秒周期且首次失败即复检，避免“通用网页可达、Codex 实际不通”的假健康
- ✅ 兼容/严格两种模式都补强大陆 DNS 引导：节点域名使用加密直连解析，AI 域名使用海外 DoH 并强制走代理；增加 TCP 多 IP 并发和 keepalive
- ✅ 现有 1.19.25 内核已隔离验证新配置语法；官方最新 1.19.30 会由后台更新链路下载、验签并在下次用户主动重启代理时应用

### v2.4.8 (2026-08-27)
- ✅ 订阅下载增加五种 Clash/Mihomo 客户端标识并在同一总超时内有界轮换；配置代理损坏后的直连兜底也会继续换标识，不再只尝试一次
- ✅ HTTP 200 网页/拦截页会在下载层立即识别为无效配置，自动改走已有节点；403/406、限流、地区限制、DNS/TLS/网关等可恢复错误会按线路切换
- ✅ 一次性 Mihomo 改为按目标订阅结果确定性轮换最多 5 个节点，并补充强关联离线缓存或程序保存的最后节点；单节点失败不会提前结束整个更新
- ✅ 实机验证 5 节点逐项切换、真实订阅下载、临时进程与凭据清理均正常，主代理 PID、配置和代理环境全程不变

### v2.4.7 (2026-08-27)
- ✅ 订阅常规/直连下载失败后，懒读取现有受管节点池并启动一次性隔离 Mihomo，通过端到端健康回退组中的可用节点再次拉取
- ✅ 隔离配置强制所有订阅域名走节点，避免兼容分流的 `MATCH,DIRECT` 形成假兜底；显式本机代理也不会被 `NO_PROXY` 绕过
- ✅ 正常下载不读取节点配置；兜底完成后立即退出并清理临时凭据目录，不切换或重载当前代理，也不改系统代理、环境变量和 Codex 会话

### v2.4.6 (2026-08-26)
- ✅ 订阅链接拉取前检查 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 等回环代理；端口失效且能确认由本程序写入时，在生命周期锁内强制复检后自动清理
- ✅ 自动清理后当前订阅请求仍固定直连，避免 Windows/urllib 的旧代理缓存把同一次请求重新送回失效端口
- ✅ 正在启动或切换的受管代理不会被清理；来源不明的代理变量只临时绕过并明确提示，严格隐私模式继续禁止直连

### v2.4.5 (2026-08-25)
- ✅ API、网络诊断与远程 Codex 实测增加响应体、流式单行和累计数据上限，异常网关不再无限占用内存；失效代理探测缓存也改为有界缓存
- ✅ SSH 命令同时排空 stdout/stderr，避免大输出互相阻塞，并为命令输出和远程文件读取增加超时及大小保护
- ✅ 应用日志按大小自动轮转，本地代理手动导入与缓存订阅统一限长，损坏或被篡改的超大缓存不会拖慢启动
- ✅ Git 历史文件统计由逐提交启动进程改为单次批量读取并缓存，重复刷新耗时由秒级降至约 50 毫秒
- ✅ Claude/Codex 自动填写、URL 嗅探、账号切换、SSH 与代理路径完成专项和全量回归，并通过隔离配置的 Windows 打包实机启动验证

### v2.4.4 (2026-08-25)
- ✅ 代理自启会保留现有故障切换池；旧版本已退化为单节点时，仅在节点指纹精确匹配当前订阅的前提下，从受管离线缓存重建最多 5 节点池
- ✅ mihomo 启动失败只展示本次启动诊断，历史日志读取和轮换均严格限长；已校验的新内核继续暂存到用户主动重启时应用，避免打断 Codex/Claude 会话
- ✅ 大型官方账号快照在 Windows 上主动改用 DPAPI，避免 Credential Manager 容量错误；API 测试会在联网前拦截带中文说明、换行或控制字符的错误密钥
- ✅ SSH 同一服务器的并发任务复用单一连接，常规远端断连不再计作应用错误；浏览器自定义 URL 与统计会话计时进一步加固
- ✅ 显式/便携数据目录不再被旧项目 `storage` 静默填充；隔离实机启动达到 0 错误、0 警告且不修改真实代理、账号和 SSH 配置

### v2.4.3 (2026-08-25)
- ✅ Toast 增加成功/处理中/注意/失败语义、标题、动态阅读时长、悬停暂停、手动关闭、重复合并和最多三条堆叠，长反馈不再瞬间消失或互相覆盖
- ✅ 全局状态栏区分硬失败与“失败但已回滚/保留原节点”的安全降级，并在显示前自动遮蔽常见 API Key、Token 与 URL 查询密钥
- ✅ Win11 代理检查和连通性测试按真实结果着色；只读失败明确提示未修改本机设置，启动/停止失败明确提示回滚或核对范围
- ✅ API 自动解析反馈会说明识别到的类型、Provider、认证方式、URL 补全和模型处理；解析失败保证表单不变并列出兼容格式
- ✅ API 测试结果明确展示失效代理的处理动作，并区分自动清理、切换保护期和手动清理后的终端/VS Code 生效范围

### v2.4.2 (2026-08-24)
- ✅ 本机代理启动、热更新、停止、内核更新与启动修复增加跨线程/跨进程互斥，多开软件或后台任务不再交错重载、停止或恢复代理设置
- ✅ 失效代理自动清理改为锁内强制复检；运行中或启动/切换保护期只临时直连，不删环境变量
- ✅ 不再用当前进程的旧代理值删除用户后来写入注册表的新值，并修复自动清理后同一 API 请求又绕回失效 WinINET 代理的问题
- ✅ mihomo 配置与 PID 改为原子发布；启动/更新内核前先用实际待启动内核做配置预检，新进程失败会自动清理而不留孤儿进程

### v2.4.1 (2026-08-24)
- ✅ Win11 代理改为最多 5 节点的 mihomo 端到端健康回退池，主节点超时后新连接无需重启内核即可转到可用备用节点
- ✅ 代理状态区分“进程在运行”和“上游端到端健康”，可显示当前是否已切到备用节点
- ✅ 补齐本工具拥有但意外缺失的 Windows 代理变量；确认受管代理已退出时自动恢复之前的环境、VS Code 和系统代理，用户自定义非空值不会被覆盖
- ✅ 本机 mihomo 正常运行改用静默日志和控制器健康状态，避免运行日志无限增长及长期保留访问目标
- ✅ 发布测试增加 Git 临时目录边界，上次异常中断的快照测试不会污染后续临时仓库

### v2.4.0 (2026-08-13)
- ✅ 增加本机/SSH mihomo 内核版本检查、摘要与平台自检、原子更新和安全回退；运行中只暂存更新，避免打断 Codex/Claude 会话
- ✅ 修复全新 SSH 服务器因“隔离验证先于内核安装”导致的首次代理部署失败
- ✅ pytest 默认使用一次性数据目录，测试不会再读写真实代理状态或拉起/停止真实 mihomo
- ✅ 修复代理订阅链接拉取兼容性，失败时保留已验证的本机缓存
- ✅ Win11 代理页支持导入本地 Clash/mihomo YAML，并在重启后恢复全部节点
- ✅ 自动选优与热更新继续排除香港节点，香港节点仅保留手动选择
- ✅ 本机节点测速升级为连续 TCP + 三轮 AI 服务短探针 + 完整上下行/compact 路径深测，验证失败不改变原选择或当前运行节点
- ✅ 修复 SSH 代理跨分组异步拉取串写，节点选择精确保存到目标分组
- ✅ 修复远程 Codex 账号拉取、推送与切换，支持远端 `CODEX_HOME` 和凭据存储差异
- ✅ 远程 Codex 官方账号切换增加写后验证与事务回滚，不再误删无关 Provider 密钥
- ✅ 发布构建锁定完整依赖版本，并增加缓存/状态及配置读取失败的数据保护

### v2.3 (2026-05-11)
- ✅ 区分第三方 API Profile 与官方账号快照，Claude/Codex 均支持两类切换
- ✅ 增加切换前预览、配置健康检查和更清晰的当前运行状态识别
- ✅ 增强 SSH 推送/拉取和远端自动续跑管理，支持远程自定义路径、后台执行和部分失败汇总
- ✅ 恢复并加固浏览器 Profile 跨机器迁移，支持托管 Profile 登录态数据导出/导入
- ✅ 增强浏览器 Profile 隔离启动和占用检测，减少无关浏览器窗口造成的误拦截
- ✅ 优化 UI 弹窗位置、浏览器 Profile 编辑体验和最终打包流程

### v2.2 (2026-05-08)
- ✅ 新增 Git 版本管理功能（默认开启）
- ✅ 自动创建 Git 快照（对话开始、错误恢复、自动续跑）
- ✅ 智能初始化 Git 仓库
- ✅ 自动配置 Git 用户信息
- ✅ 支持查看提交历史和回滚
- ✅ 完善错误恢复脚本集成

### v2.1 (2026-05-08)
- ✅ 完善 DeepSeek V4、Kimi、GLM provider 预设
- ✅ 支持 provider 独立模型选择和推理力度处理
- ✅ 优化 Codex 第三方 `[model_providers.<id>]` 写入
- ✅ 增强配置存储、密钥清理、TOML 读取和远程拉取鲁棒性
- ✅ 增加测试连接、刷新模型列表和 provider 专属提示
- ✅ 增强错误自动恢复功能
- ✅ 支持 10 种错误类型识别
- ✅ 5 种智能恢复策略
- ✅ 详细的错误统计和分析
- ✅ 优化用户界面
- ✅ 改进错误处理和容错性

### v2.0 (2026-05-08)
- ✅ 添加错误自动恢复功能
- ✅ 支持 Claude Code 和 Codex CLI
- ✅ 自动检测和处理 API 错误

### v1.0
- ✅ 基础 API 管理功能
- ✅ 配置切换功能
- ✅ 自动续跑功能

## 📄 许可证

本软件仅供个人使用。

## 🙏 致谢

感谢所有 API 提供商和开源社区的支持。

---

**版本**: v2.4.10
**发布日期**: 2026-08-27
**开发者**: API切换器团队
