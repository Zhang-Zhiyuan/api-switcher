# v2.4.55 全量回归与界面实测

检查日期：2026-09-21 至 2026-09-22。Windows 11、Python 3.12.10、CustomTkinter 5.2.2、PyInstaller 6.21.0。

## 本轮修复

1. 会话迁移页的统计信息从筛选按钮旁移到独立整行，按可用宽度换行；反复切换宽窄布局时明确重设跨列数，避免文字截断。
2. 主窗口在中等宽度下将四个全局按钮排成一行；窄屏收起重复的应用标题说明，为可滚动正文保留空间。
3. 底部反馈栏在布局时优先保留，防止较矮或高 DPI 窗口把状态文字挤掉。
4. 主窗口使用 CustomTkinter 的布局入口更新缩放缓存，修复调整 DPI 后按钮被还原成旧单行布局的问题；一次布局使用同一个宽度快照。
5. 新增隔离截图工具及 8 项真实窗口用例。修正旧弹窗预览脚本，默认只截取已映射的目标窗口，不在窗口隐藏时误截桌面。

## 回归结果

- 本轮共 **3976 项测试通过，1 个手动联网测试模块跳过**。
- 后端及非原生窗口基础回归：3671 项通过；包含使用隔离目录、合成凭据的真实 Codex CLI 格式检查，以及真实 mihomo 的回环故障切换实验。
- 原生窗口：16 个独立进程中的模块，共 305 项通过；包含本轮新增 8 项。
- 修改完成后另行复测 54 个相关非原生窗口与发布模块：941 项通过、1 个手动模块跳过；这些重复验证不重复计入总数。
- Ruff、Python 语法、中文编码、Git diff 空白检查、32 项固定版本依赖检查通过。
- v2.4.55 单文件构建和 8 秒隔离启动检查通过；直接检查安装包内代码，确认主窗口及会话迁移修复已包含，空密钥库后端也已打包。

## 逐项覆盖

| 功能 | 验证内容 |
| --- | --- |
| Claude / Codex 账号 | 切换、明文及加密导入导出、重复导入、双机隔离模拟、身份冲突、失效格式、写入失败回滚 |
| API 配置 | 自动解析与填写、URL 校验、认证字段、异步测试、敏感信息脱敏 |
| SSH | 配置及账号拉取/推送、事务保护、失败恢复、目标服务器变化 |
| Win11 / WSL 代理 | 受管进程判断、部署互斥、设置恢复、脏代理清理边界、WSL 集成回滚 |
| 订阅与节点 | 下载重试及超时、缓存保护、定时刷新、改名/改链接/删除竞态、全量测试范围、取消与未完成结果 |
| 分流 | 家宽/非家宽默认策略、手动绑定优先、候选池、固定节点、自动切换、批量草稿及保存 |
| 其他页面 | 环境变量、浏览器 Profile、会话迁移、通用设置、使用统计、备份、日志、自动续跑及托盘相关回归 |
| 界面 | 实际窗口、列表增量渲染、筛选和勾选、宽窄布局、高 DPI、反复缩放、关闭/重开、异步失败 |

## 截图

合成账号、订阅、节点和服务器数据；禁用自动部署、联网、真实密钥库和持久系统设置写入。

- 全部 11 个主页面：宽屏 1120×800、窄屏 740×720、高 DPI 960×760，分别检查页首和页尾；SSH/Win11 另查页面中部。
- 高 DPI 场景额外设置 1.5 倍控件缩放；极窄窗口另测 480×600。这里的窗口尺寸是逻辑尺寸，截图物理像素随系统 DPI 增大。
- 分流、候选池及批量设置窗口分别检查宽/窄布局；另检查账号导出、导入结果和可选密码窗口。
- 保留 83 张截图在 `dist/ui-audit-20260921/after/`。主页面报告中回调异常和被阻止的外部操作均为 0。
- 额外的前台屏幕像素对照因窗口未取得系统前台焦点而被安全拒绝，未读取桌面、未计入上述截图；后台原生窗口截图的滚动区可能有 PrintWindow 重绘残影，因此布局边界另由真实 Tk 控件几何断言验证。
- [分流窗口截图](dist/ui-audit-20260921/after/dialogs/routes-narrow.png)
- [候选池截图](dist/ui-audit-20260921/after/dialogs/node-pool-narrow.png)
- [账号导入结果截图](dist/ui-audit-20260921/after/dialogs/account-transfer-imported.png)
- [会话迁移布局截图](dist/ui-audit-20260921/after/wide-会话迁移-top.png)

## 边界

没有切换当前真实账号、重启现用代理、写入真实 SSH/WSL 设置或向 OAuth 服务端刷新 token。测试通过不代表所有真实订阅、远端网络和登录凭据始终有效；已撤销、过期或轮换后的登录包仍需重新登录生成。

## 安装包

- `dist/API切换器.exe`，版本 `2.4.55`
- 大小：`31167183` 字节
- SHA-256：`30350752F328A34B337650BD6643D1EE2477078C1A0E12C5D881FED9BD894639`

复现：`python release_check.py --build`；截图：`python tools/ui_visual_audit.py --view compact --label verify`。
真实客户端和内核用例需设置 `API_SWITCHER_TEST_CODEX_CLI` 与 `API_SWITCHER_MIHOMO_TEST_CORE`；本轮基础回归已设置这两个入口。
