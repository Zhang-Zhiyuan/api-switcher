# v2.4.54 检查记录

日期：2026-09-21。环境：Windows 11、CPython 3.12.10、PyInstaller 6.21.0。

## 结果

- 全量回归及新增用例共 **3968 项通过**，**1 个手动真实 API/GUI 测试模块跳过**，无未解决的测试失败。
- 计数：后端与非原生窗口基线 3655 项 + 15 个独立进程中的原生窗口用例 297 项 + 本轮新增 16 项；复测不重复计数。最终源码收集结果为 3968 项。
- 最终源码另行复测 55 个 UI 相关非原生窗口模块：949 项通过、1 项手动测试跳过。改动相关 108 项回归通过。
- Ruff、全项目语法、中文编码、Git diff 空白检查、32 个固定版本依赖检查通过。
- 单文件构建及 8 秒隔离启动验证通过；检查安装包内的 Claude/Codex 刷新代码，确认包含本轮任务标记修复，而非旧缓存版本。

## 本轮修复

1. Claude/Codex 后台刷新结果无法调度回界面时释放忙碌标记，支持再次刷新及切页恢复；后台线程不调用 Tk 作为替代方案。
2. 完成回调按任务处理一次，迟到或重复的回调不影响新任务；已销毁页面不再启动刷新。
3. 发布检查自动按模块隔离真实 Tk 测试，避免原生窗口全局状态跨模块污染；分批运行仍保留完整测试收集，单批失败后继续检查其余批次。
4. 打包试启动与回归测试使用相同的 API/代理环境变量隔离规则，不修改父进程环境。

## 逐项覆盖

| 功能 | 核对场景与结果 |
| --- | --- |
| 账号切换与登录包 | Claude/Codex、明文/加密包、双机模拟迁移、旧凭据保护、重复导入、身份冲突、写入失败回滚；通过 |
| API 配置 | 自动解析/填写、URL 校验、供应商及认证配置、异步测试、敏感信息脱敏；通过 |
| SSH 同步与配置 | 拉取/推送事务、失败恢复、服务器改名/删除后的分流关联；通过隔离与模拟测试 |
| Win11/WSL 代理 | 操作互斥、受管进程识别、代理设置恢复、WSL 集成及回滚；通过隔离与模拟测试 |
| 订阅下载/定时更新 | 多路径重试、重定向与总超时、失败保留缓存、并发改名/改链接/删除、定时重排与草稿保护；通过 |
| 分流与自动切换 | 家宽/非家宽默认分配、手动选择优先、固定节点、自选候选池、批量修改、严格模式；通过 |
| 节点检测 | 全量/筛选范围、取消和未完成结果、缓存有效性、进度反馈、真实内核批量检测；通过 |
| UI | 真实窗口的筛选、批量勾选、草稿保存、窄屏/高 DPI、异步失败、关闭与重开；通过 |
| 其他模块 | 备份迁移、浏览器 Profile、会话迁移、日志、使用统计、自动续跑、托盘与启动；现有自动化回归通过 |

## 实测边界

- Codex CLI 0.154.0-alpha.6.2 使用合成 token 和隔离目录验证本地登录文件格式；**未向服务端刷新或验证真实账号**。
- mihomo v1.19.30 使用独立配置、回环端口及合成上游，实测分流、批量检测和节点失效后的候选池内切换；确认不会转入未选节点、默认家宽或 DIRECT。
- 账号/API 页面以 20 个合成 API 配置及 20 个合成账号实际运行并截图，回调错误为空。结果保存在 `dist/ui-performance-v2.4.54/`；功能正确性验证与性能测量分开解释，不把并行测试时的耗时当作稳定性能承诺。
- 未切换现用账号、未重新部署现用代理、未写入真实 SSH/WSL 环境。真实远端网络、订阅服务可用性以及 OAuth 凭据是否被撤销/轮换，不由离线测试保证。

## 发布文件

- 文件：`dist/API切换器.exe`
- 版本：`2.4.54`
- 大小：`31166676` 字节
- SHA-256：`395DD21A8F72DAE95C594465296E814D198D2D509328544B2FA877123C4F043C`

常规复现入口为 `python release_check.py --build`。真实客户端/内核用例需将 `API_SWITCHER_TEST_CODEX_CLI`、`API_SWITCHER_MIHOMO_TEST_CORE` 指向相应可执行文件；本轮两个入口均已配置，相关集成测试未跳过。
