# v2.4.57 目标分流直连选项

日期：2026-09-26。承接工作区已有改动；本轮未提交或推送 Git，未替换用户正在运行的程序。

## 功能

- Win11、SSH 共用的目标分流编辑器新增“直连（不经过代理）”，与家宽、非家宽等订阅选项并列。选择直连会启用该目标，并解除原订阅、固定节点或候选池绑定；统一保存并应用才生效。
- 批量设置支持直连，同时启用勾选目标；其他位置及未勾选目标不变。单个自定义域名、IPv4/IPv6 CIDR 和自定义默认线路也支持直连。
- 以独立 `service_route_modes: direct` 保存意图，不冒充订阅、不自动回退代理；重新打开、标记建议、缓存刷新、旧版分流整理和 SSH 热更新保留手动选择。
- 生成显式 `DIRECT` 规则，即使默认启用了大陆外代理也优先生效。更具体的 AI / 自定义域名保持自身线路；直连 DNS 使用系统解析，并为子域名保留明确覆盖项。
- 严格隐私与有效直连规则冲突时拒绝应用，不静默关闭隐私保护。Windows 保存前检查冲突，SSH 热更新失败恢复原绑定和配置；停用目标仅保存选择，不生成直连规则。
- 总览、修改清单和只读运行诊断显示直连，不再提示选择代理节点。

## 验证

- 代理、路由、订阅、WSL 及真实内核相关非原生窗口回归：**1837 passed**。
- 7 个原生窗口模块分别在独立进程运行：**149 passed**（目标默认选择 23、编辑器 32、总览 18、批量 38、候选池 14、旧版整理 15、诊断窗口 9）。
- 额外定向复测直连、节点池、打包工具和发布检查：**142 passed**；部分与上述重复，不累加为独立总数。
- 真实 mihomo 在独立端口、独立配置及回环假上游运行：YouTube / Google / 自定义目标收到直连端点响应，Claude / Google AI / 显式自定义代理例外仍收到代理上游响应。所有测试域名静态映射回环；未访问真实网站或真实 SSH 服务器。
- Win11/SSH 保存重开、批量启用、订阅重名冲突、DNS、规则优先级、严格隐私拒绝和回滚均有回归测试。
- Ruff 全仓库、Python 语法、中文编码、32 项锁定依赖及 Git diff 检查通过。
- 本轮为上述定向回归，不宣称重新执行了账号等无关功能的全仓库测试。

## 界面截图

- [宽屏](dist/direct-routes-ui/direct-1040x760-1.0.png)
- [窄屏](dist/direct-routes-ui/direct-720x680-1.0.png)
- [额外 1.25 倍控件缩放](dist/direct-routes-ui/direct-860x720-1.25.png)

截图来自真实 CTk 合成数据窗口，已检查实际窗口尺寸、按钮边界、直连显示和节点控件禁用状态；只截取该窗口，不读取桌面或真实凭据。

## 成品与安全边界

- `dist/API切换器.exe`，版本 **2.4.57**，**31173030** 字节。
- SHA-256：`3C436472E20E21BBA669ED4422D2FEEF87184C0460DC50131A50BB09D9410EF3`。
- PyInstaller 构建、8 秒隔离启动检查通过；已核对成品内嵌的单项/批量直连选项、隐私冲突校验和规则处理代码。
- 构建临时目录与自动生成的 spec 已清理，可重新生成。
- 未切换真实账号、修改注册表/环境代理、连接真实 SSH 或重启现用 mihomo；检查前后原程序及代理进程 PID 与启动时间一致。
- 直连依赖目标设备自身可达性，不承诺大陆网络可以直连 YouTube。WSL 共享时由 Windows 内核出站；SSH 则由对应服务器出站。严格隐私需要用户明确调整后才能启用直连。

使用：打开新版程序 → 管理目标分流 → 找到 YouTube 等目标 → 访问线路选择“直连（不经过代理）” → 保存并应用；也可在批量设置中一次操作多个目标。
