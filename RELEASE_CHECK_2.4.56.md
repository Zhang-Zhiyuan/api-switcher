# v2.4.56 代理稳定性与 SSH 快测优化

日期：2026-09-22。此版本承接 v2.4.55，不自动改写正在运行的代理配置。

## 最终结果

- 最终覆盖 **4026 项通过，1 个手动联网测试模块跳过**：非原生窗口部分 3714 项、17 个独立原生窗口模块共 312 项。
- 完整 `release_check.py --build` 通过依赖、语法、中文编码、Ruff、Git diff、构建及 8 秒隔离启动检查。
- 完整检查中有一个旧滚动条用例因重复创建 Tk 根窗口而跳过；已修正为共享测试根窗口，单独复测该模块 **33 项全部通过**，上述最终覆盖数计入这次补测、不重复计算。最后仅修改测试基础设施，运行时代码与安装包保持一致。
- 提前执行的代理专项和 39 项真实内核/下载兜底实验也通过；属于重复验证，不另加进总数。
- 已读取成品内嵌代码，确认包含 SSH 流式回调、新探针状态码和后台维护提示。
- 本轮没有提交或推送 Git；此前工作区中的修改保留。

## 改动

- SSH 基础快测从多次小批命令改为单次命令：远端工作池持续补充空闲槽位，结果按完成顺序返回；保留同端点复用和每个原始节点的结果。
- SSH 输出按增量 UTF-8 解码，stdout/stderr 轮流读取，持续输出也受总时限和字节上限约束；取消只关闭本次命令通道，不断开共享连接。超时、异常和取消均保留已经收到的完整结果。
- 远端任务只提交一个并发窗口。输出管道关闭后不再提交新节点；已开始的检测仍受单节点预算限制。
- Claude 自动探针改为 `200/400/401`，Gemini 为 `200/400/401/403`，不再把全部 4xx 当作健康。Google 的无凭据 403 保留，诊断明确提示它仍需业务验证；不能仅凭这个状态码推断账号、地区或模型可用。
- Win11 与 SSH 严格探针检查结构化认证错误，不把 HTML 错误页或带鉴权关键词的地区/权限拒绝误判为正常认证挑战。
- 旧版 Claude/Gemini `200-499` 配置仅在受管/严格隐私识别时兼容，新生成配置不再采用；不因探针升级错误判定原有隐私保障失效。
- Win11 选项改名为“退出后仅保留代理内核”；运行设置和关闭窗口明确区分托盘维护、直接退出和保留内核。

## 验证范围

- 健康检查覆盖正常认证挑战、403 限制、429、5xx、畸形 JSON、HTML 错误页，以及 Windows/SSH 严格探针结果一致性。
- SSH 覆盖分段中文输出、重复及未知节点、截断/缺失结果、超长行、超时、取消、输出上限和已完成结果保留；模拟慢节点验证空闲槽位能够继续推进。
- 真实 mihomo 使用独立配置、独立控制口和纯回环假上游，验证三种 AI 状态码契约、候选池故障切换、实际分流和批量检测。不使用真实订阅或 API 密钥。
- 原生窗口检查关闭弹窗的宽窄布局、额外 1.5 倍控件缩放、按钮可见性和关闭选择；Win11 运行设置另行截图检查。

## 截图

- [窄屏高 DPI 关闭提示](dist/proxy-review-20260922/proxy-lifecycle-420x300-scale-1.5.png)
- [Win11 运行设置](dist/ui-audit-20260921/verify/compact-Win11%20代理-maintenance.png)
- [高 DPI 运行设置](dist/ui-audit-20260921/verify/scaled-Win11%20代理-maintenance.png)

截图只读取合成数据窗口，不截取桌面或真实凭据；预览阻止联网和持久系统设置写入。

## 使用与边界

- 打开新版程序后，提示和 SSH 快测立即使用新版逻辑；已经部署的内核配置不会后台强制替换。需要升级该配置的探针规则时，再使用相应的“应用规则”操作。
- 需要订阅自动维护持续运行时，应最小化到托盘。此轮没有安装额外后台服务，直接退出后的订阅刷新仍暂停。
- 此轮未实现订阅成功下载路径记忆或独立后台维护服务；没有把这两项计作已完成。
- 未切换真实账号、重启现用代理、连接真实 SSH 服务器或修改真实 Windows/WSL 代理环境。模拟 SSH 调度验证不是实际公网性能测量，不能据此保证固定提速倍数。

## 成品

- `dist/API切换器.exe`，版本 `2.4.56`，`31169337` 字节。
- SHA-256：`1A739A65B00ACA1EF294675604EF0B3D5E0334ED6438413C45BF4A3A0B171E6D`。
- 构建临时目录和自动生成的 spec 已清理，可重新构建；真实账号、订阅缓存和正在运行的程序未替换。

复现：`python release_check.py --build`；真实内核与客户端格式用例需分别设置 `API_SWITCHER_MIHOMO_TEST_CORE` 和 `API_SWITCHER_TEST_CODEX_CLI`，本轮两者均已启用。
