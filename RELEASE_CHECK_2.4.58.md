# v2.4.58 分流保护、目标探针与保存前预览

日期：2026-09-27。环境：Windows 11、Python 3.12.10、CustomTkinter 5.2.2、PyInstaller 6.21.0。

## 改动

- SSH 本机记录缺失时，保护远端已配置的直连、自定义目标、独立订阅和新版记录中的停用 / 手动默认选择，避免普通刷新用空配置覆盖。首次应用失败时恢复“原本没有记录”，不留下错误的空白授权记录。
- 新版受管配置保存不含订阅凭据和节点定义的分流恢复注释。编辑器新增“从远端恢复本地分流记录”：只读远端、只补建缺失的本地记录；已有本机记录不覆盖。验证格式、大小、重复字段、记录校验值与规则校验值；校验值只检查一致性，不是签名或加密。
- 旧远端没有恢复记录时不猜测订阅绑定。首次手动重建前提示覆盖范围；恢复后仍可能需要重新导入对应订阅及缓存。默认空配置不添加无用恢复注释，只有恢复注释变化时只保存文件、不热重载内核。
- 为 YouTube、Google、GitHub、Hugging Face、X、Reddit、Discord、Telegram 绑定独立线路后，各自使用本站无凭据 HTTP 端点；不同目标即使共用订阅也分别检查。普通网站周期为 30 秒，AI 保持 10 秒，兼容旧 10 秒配置的严格隐私识别。跟随全局默认线路的目标仍沿用默认组探针，直连不使用代理探针。
- 自定义目标仍使用通用网络探针，反馈明确区分通用连通与目标网站 HTTP 可达；均不保证登录、视频、文件下载、消息业务或长会话成功。失败节点仍只在手动选定的订阅 / 候选池内切换，不自动切成直连。
- 修改清单增加同目标覆盖、子域名和更小 IPv4/IPv6 网段例外预览；与实际构建共享规则归属，避免两套优先级。已知严格隐私冲突在批量提交前拦截；SSH 未知隐私状态由应用阶段再次核验。
- 展开规则预览时收起重复说明，为窄屏和高 DPI 下的可滚动列表保留空间。预览是草稿分析，不冒充实时运行状态。

## 验证

- 最终用例收集：**4123 项**。完成全量回归及失败相关模块复测，最终覆盖 **4123 项通过**、**1 个手动联网模块跳过**，无未解决的测试失败。
- 非原生窗口部分最终覆盖 3798 项，18 个独立原生窗口模块共 325 项。首轮发现 SSH 旧占位配置测试、恢复注释引起的多余重载，以及原生窗口尚未显示就点击的测试时序问题，均已处理后复测；并非把首次失败当成通过。
- 最后一次代理 / 路由 / 订阅 / WSL / 内核专项：**1890 passed**，覆盖修改后的运行代码、全部 4 个原 SSH 失败项以及后续增加的恢复记录完整性和注释更新测试。重复复测不累加总数。
- 原生滚动回归模块修正测试前置条件：等待 CTk 标题栏重绘后的窗口显示，再模拟点击，避免窗口仍处于临时隐藏状态时再次切换外观。完整模块 **33 passed**，另在 3 个独立进程重复通过；没有放宽滚动断言。
- 真实 mihomo 使用独立临时配置、控制端口和回环假上游，验证 AI / 网站 HTTP 状态码契约、直连与代理例外、故障切换；不使用真实订阅节点。真实 Codex CLI 只在隔离目录使用合成凭据检查登录文件格式，不验证实际服务端登录。
- 新恢复 / 预览窗口 **7 项原生测试通过**，检查单服务器恢复、失败保留草稿、多位置隐私拦截及 3 种尺寸 / 缩放。恢复操作未调用远端写入或运行配置应用。
- 全项目 Ruff、语法、中文编码、Git diff 空白检查与 32 项固定版本依赖验证通过。全量测试发现问题后，修复相关复测与构建检查分别执行。

## 界面证据

- [1040×760](dist/route-preflight-ui/preflight-1040x760-1.0.png)
- [720×680](dist/route-preflight-ui/preflight-720x680-1.0.png)
- [860×720、额外 1.25 倍控件缩放](dist/route-preflight-ui/preflight-860x720-1.25.png)

截图只截取合成数据的原生窗口，不包含真实订阅或桌面内容；测试校验实际窗口尺寸，保存按钮和预览边界可见。更长的列表与清单通过滚动查看。

## 成品与安全边界

- `dist/API切换器.exe`，版本 **2.4.58**，**31184373** 字节。
- SHA-256：`13FC1D6AAA7FE5BECFF96AD4FE5B85F890283FA9567B8A53A29A574E8661F20F`。
- PyInstaller 构建、8 秒隔离启动及产物检查通过。已读取成品内嵌代码，核实包含恢复入口、记录校验、规则预览、目标网站探针与只更新注释不重载的逻辑。
- 本轮承接此前未推送的账号迁移、界面性能、SSH 测速与直连功能修复，一并进入此版本。
- 未连接真实 SSH、切换真实账号、改写系统代理 / 环境变量、操作 WSL 或重启现用代理；原程序与 mihomo 的 PID、启动时间与此前记录一致。只清理可重新生成的构建目录、spec 和缓存。
- 公开端点的本机无代理 HEAD 抽查中，Google 返回 204，GitHub / Hugging Face / X / Reddit 返回 200；Discord 超时，Telegram 证书主机名校验失败。未关闭 TLS 校验，也未据此声称这些业务在所有网络或节点上可用；实际结果取决于选定节点及目标服务。

配置策略参考：[mihomo fallback](https://wiki.metacubex.one/en/config/proxy-groups/fallback/)；Discord 无凭据端点参考：[Gateway 文档](https://docs.discord.com/developers/events/gateway)。使用新功能需打开新版程序；已有运行内核不会因本次打包而被自动重载。
